<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mindidea.feelfit"
    android:versionCode="3"
    android:versionName="1.6.1"
    android:installLocation="preferExternal" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="inputWeight"></activity>
        <activity android:name="display"></activity>
        <activity android:name="contact"></activity>
        <activity android:name="graph_history"></activity>    
        <activity android:name="graph_myGraph"></activity>
        <activity android:name="fbSharedDialog"></activity>
        <activity android:name="comfirm_stop"></activity>
        <activity android:name="NotificationReceiver"></activity>
        <!-- <service android:enabled="true" android:name="MyService"></service> -->
        <activity android:name="TestConnect"></activity>
        <activity android:name="TestPost"></activity>
  </application>

</manifest>